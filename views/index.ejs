<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Join Chat</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    .card {
      border-radius: 20px;
      box-shadow: 0px 8px 20px rgba(0,0,0,0.2);
    }
    .card-header {
      border-radius: 20px 20px 0 0;
      text-align: center;
      font-size: 1.5rem;
      font-weight: bold;
      background: #ffffff;
      color: #2575fc;
    }
    .btn-custom {
      background: #2575fc;
      color: #fff;
      font-weight: 500;
      border-radius: 12px;
    }
    .btn-custom:hover {
      background: #1a5fd6;
    }
    input {
      border-radius: 12px !important;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="row justify-content-center">
      <div class="col-md-6 col-lg-4">
        <div class="card p-4">
          <div class="card-header">
            👋 Welcome to ChatApp
          </div>
          <div class="card-body">
            <form action="/login" method="post">
              <div class="mb-3">
                <label for="username" class="form-label">Enter your name</label>
                <input type="text" name="name" class="form-control" placeholder="Your Name" required>
              </div>
              <div class="d-grid">
                <button type="submit" class="btn btn-custom">Join Chat</button>
              </div>
            </form>
          </div>
          <div class="text-center mt-2">
            <small class="text-muted">Chat with friends instantly 🚀</small>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    document.getElementById("joinForm").addEventListener("submit", function(e){
      e.preventDefault();
      const name = document.getElementById("username").value.trim();
      if(name){
        // Redirect or pass the name to chat page
        window.location.href = `/chat.html?name=${encodeURIComponent(name)}`;
      }
    });
  </script>
</body>
</html>
